rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny everything by default; allow read/write only to
    // authenticated users (the shared team account).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
